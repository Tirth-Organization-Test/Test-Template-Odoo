# Standard Odoo 18.0 development configuration

This branch holds the development configuration shared by every customer
repository running **Odoo 18.0**.

Generated customer repositories start from this branch as their `main`.

## Contents
- `.pre-commit-config.yaml` - hooks run before each commit
- `.gitignore` - files never committed
- `requirements.txt` - python packages needed by the custom modules
